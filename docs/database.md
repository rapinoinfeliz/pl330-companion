# Banco de dados

`broadcasts` é indexada por frequência, início, fim, temporada, idioma, país e transmissor. `space_weather_snapshots` é indexada por fonte e coleta. Imports e status têm tabelas próprias. Todas as consultas externas são parametrizadas. O diário não existe no D1.
